__pycache__/
*.pyc
data/
*_data.json
.env
.env.*
!.env.example
# market_data.csv 는 RDS mkt100_market_daily 가 단일 정본 — 로컬 mirror 만 유지.
# clone 후 `python rds_loader.py --download` 로 재생성.
history/market_data.csv
history/market_data.csv.bak_*
history/market_data.csv.bak-*
history/market_data.csv.before_round_*
.venv/
venv/

# Runtime logs (auto_market 자동 실행 로그 등)
logs/*.log

# Fund 원본 파일 — S3 에 올린 후 로컬 보관만. git 에는 index.html 만 커밋.
output/fund/*
!output/fund/index.html

# S3 에서 받아온 로컬 다운로드 (PDF 등)
downloads/

# Claude Code
.claude/settings.local.json

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
